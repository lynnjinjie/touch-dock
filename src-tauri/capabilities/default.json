{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the TouchDock main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
